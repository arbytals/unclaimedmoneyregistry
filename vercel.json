{
  "buildCommand": "pnpm install && pnpm playwright install chromium --with-deps && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "app/api/search/route": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}

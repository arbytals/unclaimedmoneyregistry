{
  "buildCommand": "rm -rf .next && rm -rf node_modules/.cache && pnpm install && pnpm exec playwright install chromium --browser chromium && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "app/api/search/route": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}

{
  "buildCommand": "pnpm install && pnpm exec playwright install chromium && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install -E @shadcn/ui",
  "framework": "nextjs",
  "functions": {
    "app/api/search/route": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}

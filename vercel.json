{
  "buildCommand": "pnpm install && PLAYWRIGHT_BROWSERS_PATH=/tmp/ms-playwright pnpm exec playwright install chromium && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "app/api/search/route": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}

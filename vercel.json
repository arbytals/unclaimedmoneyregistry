{
   "buildCommand": "pnpm dlx shadcn-ui@latest init --yes && pnpm install && pnpm playwright install chromium && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "functions": {
    "app/api/search/route": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}